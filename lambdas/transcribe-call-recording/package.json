{
  "scripts": {
    "build": "tsc && rm -rf lambda.zip && zip -r lambda.zip .",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@aws-sdk/client-transcribe": "^3.670.0",
    "typescript": "^5.6.3",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/uuid": "^10.0.0"
  }
}
